# components

Componentes de apresentação React (Server e Client Components de UI).

Não devem concentrar regras de negócio de domínio — apenas renderização, estado de interface local e chamadas a casos de uso já validados no servidor. Ver `docs/ARCHITECTURE.md`.

## Status nesta fase

Vazio por design. As primeiras telas nascem na Fase seguinte (fluxo treinador-atleta-treino-feedback-calendário), seguindo a Interface Architecture & Screen Specifications v1.4 e a Especificação Visual de Telas (`docs/enky-os/visual-specs/`).
